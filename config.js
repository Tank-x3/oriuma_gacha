// ==========================================
// オリウマ ガチャシミュレーター 設定ファイル
// (v0.03 - 演出バランス調整版)
// ==========================================

// ------------------------------------------
// 1. 排出率設定 (%)
// ------------------------------------------
const RATES = {
    R4: 0.1, // ★4 (シークレット)
    R3: 4,   // ★3
    R2: 15,  // ★2
    R1: 80.9 // ★1
};

// 10連ガチャの「10枠目（確定枠）」の確率
const GUARANTEED_RATES = {
    R4: 0.1, // ★4
    R3: 4,   // ★3
    R2: 95.9 // ★2 (残りすべて)
};

// ------------------------------------------
// 2. 昇格演出の発生確率 (%)
// ------------------------------------------
const PROMOTION_CHATES = {
    // --- ★2の演出 ---
    HIDE_R2: 25,        // ★2なのに★1表示 (Pattern A)

    // --- ★3の演出 ---
    HIDE_R3_WEAK: 25,   // ★3なのに★2表示 (Pattern B)
    HIDE_R3_STRONG: 15, // ★3なのに★1表示 (Pattern C: 2段階昇格)

    // --- ★4の演出（※★4は100%昇格演出となります）---
    // ここでは「開始時のレアリティ」の割合を決めます
    R4_START_FROM_R3: 40, // ★3演出からスタートして★4へ (残り60%は★2からスタート)
};

// ------------------------------------------
// 3. ゲートテキスト設定
// ------------------------------------------
const GATE_TEXTS = {
    HIGH: "Eclipse first, the rest nowhere.", // 激アツ
    MIDDLE: "The favorite Star.",             // チャンス
    LOW: "A Long Shot Star."                  // 通常（ここからの逆転あり）
};

// ゲートテキストの選択ロジック（確率設定）
// 条件を満たしていても、あえて低い期待値のテキストを出してサプライズを演出します
const GATE_TEXT_SETTINGS = {
    // 【★4が含まれる場合】
    WITH_R4: {
        HIGH: 0.6,   // 60%で Eclipse
        MIDDLE: 0.35, // 30%で Favorite (サプライズ)
        LOW: 0.05     // 10%で Long Shot (大逆転)
    },

    // 【★3が含まれる場合 (★4なし)】
    WITH_R3: {
        HIGH: 0.35,   // 40%で Eclipse
        MIDDLE: 0.35, // 40%で Favorite
        LOW: 0.3     // 20%で Long Shot (逆転)
    },

    // 【★2が2枚以上の場合 (★3以上なし)】
    WITH_MANY_R2: {
        MIDDLE: 0.6, // 70%で Favorite
        LOW: 0.4     // 30%で Long Shot
    }
};